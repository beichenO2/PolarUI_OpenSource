/**
 * SSoT 节点已迁移至 ~/Polarisor/node-defs.json（生态级唯一信源）。
 * 此文件保留 export 签名以兼容旧 import，实际不再包含任何定义。
 */
export function registerSSoTNodes(): void {
  // noop — node-defs.json 是唯一信源，由 registry.loadFromUrl 加载
}
